import { QueuedSlicePage } from "../QueuedSlicePage";

export function DashboardPage() {
  return (
    <QueuedSlicePage
      title="Dashboard"
      description="Current migration slice: system status, runtime health, MCP Hub summary, and workspace metrics."
    />
  );
}
