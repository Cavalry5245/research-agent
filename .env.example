# LLM configuration (OpenAI-compatible API)
LLM_PROVIDER=openai_compatible
LLM_BASE_URL=https://api.example.com/v1
LLM_API_KEY=your_api_key_here
LLM_MODEL=deepseek-chat

# Embedding configuration
EMBEDDING_PROVIDER=local
EMBEDDING_MODEL=bge-small-zh-v1.5

# Vector store
VECTOR_STORE=chroma
CHROMA_PERSIST_DIR=app/storage/vector_db

# Storage paths
UPLOAD_DIR=app/storage/papers
NOTE_DIR=app/storage/notes
METADATA_DIR=app/storage/metadata

# MCP Configuration
MCP_ENABLED=true
MCP_STARTUP_TIMEOUT=10.0
MCP_HEALTH_CHECK_INTERVAL=30.0
MCP_TOOL_TIMEOUT=30.0

# Zotero MCP
ZOTERO_MCP_ENABLED=true
ZOTERO_MCP_AUTO_INSTALL=false
ZOTERO_MCP_COMMAND=
ZOTERO_DATA_DIR=  # Optional: path to Zotero data directory
SEMANTIC_SCHOLAR_MCP_ENABLED=false
ARXIV_MCP_ENABLED=false
RESEARCH_AGENT_MCP_ENABLED=true

# ========================================
# PDF RAG 父子文档配置
# ========================================

# PDF 解析模式: structured (推荐) | simple
PDF_PARSE_MODE=structured

# 分块策略: parent_child_sliding_window (推荐) | simple
CHUNK_STRATEGY=parent_child_sliding_window

# 父文档存储: json (默认) | sqlite
PARENT_DOC_STORE=json

# 父文档存储目录
PARENT_DOC_DIR=app/storage/parent_docs

# 子块大小（字符数）
CHILD_CHUNK_SIZE=500

# 子块重叠（字符数）
CHILD_CHUNK_OVERLAP=100

# 检索策略: hybrid (推荐) | dense | bm25
RETRIEVER=hybrid

# 是否启用 rerank
ENABLE_RERANK=true

# 是否保留页码引用
PRESERVE_PAGE_CITATIONS=true
